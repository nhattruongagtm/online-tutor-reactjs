import React from 'react';

type Props = {};

const Loading = (props: Props) => {
  return (
    <h1 className="center">
      <img src="./imgs/loading.svg" alt="" />
    </h1>
  );
};

export default Loading;
